# Generate one unconditional form-load Behavior with Runtime AI

Status: ready-for-agent
Type: task
Blocked by: 01, 03, 11, 12

## What to build

Deliver the first complete runtime-AI Design-to-ATF tracer bullet. A Test Designer publishes one valid minimal Guided Business Logic block with one unconditional form-load Outcome, clicks `Create ATF`, and receives one complete behavior-only Suite and one Test.

## Acceptance criteria

- [ ] Create exactly two production Now Assist Skills: `Extract Catalog Behavioral Contract` and `Independently Interpret Catalog Behavioral Contract`.
- [ ] Create no Now Assist Agent.
- [ ] Invoke both Skills from the bound background worker, once each for the minimal block, with the same original allowed inputs.
- [ ] Prove the Verifier never receives the Extractor output.
- [ ] Enforce the versioned structured-output schema.
- [ ] Canonicalize both responses and require exact agreement for Behavior ID, target, form-load trigger, Outcome ID, one typed scalar effect, exact literal, and source lines.
- [ ] Support the Guided Business Logic anchors and one unconditional `OUTCOME_ID` inside `LOGIC` without an `If` structure.
- [ ] Resolve the target and scalar types only from Variable Design.
- [ ] Validate the agreed contract deterministically and build one complete Generation Plan.
- [ ] Select the Outcome only from Baseline Variable State and admitted pre-open context.
- [ ] Persist one complete Suite and exactly one Test through Ticket 12.
- [ ] Impersonate the Behavior Execution User, open a fresh OOB Service Portal form, wait through bounded readiness, and assert the exact scalar effect.
- [ ] Include non-raw interpretation and Design provenance in generated descriptions.
- [ ] Create a new independent Suite on a repeated valid click.
- [ ] Never submit or order the Catalog Item.
- [ ] Prove deliberately wrong implementation behavior fails the generated assertion without changing the expected plan.
- [ ] Create zero artifacts for malformed structure, failed interpretation, disagreement, invalid Design, or failed plan derivation.
- [ ] Add Skill, canonicalization, worker, plan, writer, target-instance, and v1 regression coverage.
- [ ] Complete a clean type check and SDK build.

## Comments

